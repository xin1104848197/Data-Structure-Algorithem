/*****************************************************************
在向量中查找元素e（区间查找）：
从后向前遍历整个向量，若找出相同元素，随即返回其秩 
******************************************************************/
template <typename T>
Rank Vector<T>::find(T const &e , lo , hi)//lo<hi
{
	Rank lo ;Rank hi ;
	while ( (lo < hi--) && (elem[hi] != e) ) ; //找到相同元素返回其秩或者返回lo-1 
	return hi ;
 } 
