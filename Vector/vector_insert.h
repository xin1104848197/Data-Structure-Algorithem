/*******************************************************
将元素e作为秩为r的元素插入向量中：
以免发生覆盖，需要从后向前进行移动
1.若当前向量已满，则需先扩容
2.移动秩为r以及其后缀
3.插入e 
********************************************************/


template <typename T>
Rank Vector<T>::insert(Rank r,T const& e)
{
	expand(); //若有必要则扩容
	for( int i = _size ; i > r ,i--)
	{
		elem[i]	= elem[i-1]; //从后向前，后继元素顺次移一个单元 
	} 
	elem[r] = e ; size++ ;//置入新元素 
	return r;
}
 
